#include <iostream>
using namespace std;
int main()
{
    string a;
    cin>>a;
    if(a=="SONGDO") cout<<"HIGHSCHOOL";
    else if(a=="CODE") cout<<"MASTER";
    else if(a=="2023") cout<<"0611";
    else if(a=="ALGORITHM") cout<<"CONTEST";
    
    return 0;
}